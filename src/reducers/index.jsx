import {combineReducers} from 'redux';
// I don't think data flow should go through the store in this app
// because data is changing freqntly so I have not used 
const allReducers = combineReducers({
    
});

export default allReducers
